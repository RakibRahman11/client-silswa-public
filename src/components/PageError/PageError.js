import React from 'react';
import error from './error.jpg';

const PageError = () => {
    return (
        <div>
            {/* error page img added */}
            <img src={error} alt='' />
        </div>
    );
};

export default PageError;